/* Include File generated by Pawn Studio */

#include <sourcemod>

#if defined _hiddenmode_included_
  #endinput
#endif
#define _hiddenmode_included_

enum PlayerClass {
	TEAM_HUMAN = 0,
	TEAM_HIDDEN
}

/**
 * Returns true if the player is a hidden, false if not. 
 *
 * @param iClient			The client index.
 *  
 * @return			        True or false.
 **/
native bool HM_IsPlayerHidden(int iClient);

/**
 * Returns true if the player is a human, false if not.
 *
 * @param iClient			The client index.
 *  
 * @return			        True or false.
 **/
native bool HM_IsPlayerHuman(int iClient);

/**
 * Force to set a player's class.
 *
 * @param class			The class type. See enum PlayerClass.
 * @param iClient		The client index.
 *  
 * @noreturn
 **/
native void HM_SetPlayerClass(int iClient, int class);